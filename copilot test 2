#Write a function that takes a string as input and returns True if the string is a palindrome (reads the same forward and backward), and False otherwise.

def is_palindrome(s: str) -> bool:
    cleaned = ''.join(c.lower() for c in s if c.isalnum())  # Normalize the string
    return cleaned == cleaned[::-1]  # Check if it reads the same forward and backwards

this may be overkill for this function 